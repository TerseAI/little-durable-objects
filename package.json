{
    "name": "little-durable-objects",
    "private": true,
    "license": "MIT",
    "scripts": {
        "build": "cargo build --release && pnpm --dir npm build",
        "dev": "cargo run --release",
        "format": "cargo fmt --all && pnpm --dir npm format && prettier --write \"scripts/*.mjs\"",
        "format:check": "cargo fmt --all -- --check && pnpm --dir npm format:check && prettier --check \"scripts/*.mjs\"",
        "release:prepare": "node scripts/release.mjs prepare",
        "release:verify": "node scripts/release.mjs verify",
        "test": "node --test scripts/*.test.mjs && cargo test --locked --no-fail-fast && pnpm --dir npm test"
    },
    "devDependencies": {
        "@trivago/prettier-plugin-sort-imports": "^6.0.2",
        "prettier": "^3.8.1"
    },
    "packageManager": "pnpm@10.17.1"
}
